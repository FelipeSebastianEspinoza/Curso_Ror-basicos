 <div class="container">
     <div class="row">
         <%- if user_signed_in? %>
         <%= render @images %>
         <% end %>
     </div>
     <%= link_to 'New','images/new' , :class =>'btn btn-primary ' %>
 </div>